<footer class="bg-[--bluegray-deep] text-white mt-20">
    <div class="mx-auto max-w-7xl px-6 py-12 lg:px-12">
        
        <div class="grid grid-cols-1 gap-10 pb-12 border-b border-white/10 md:grid-cols-2 xl:grid-cols-[1.1fr,0.8fr,0.8fr,0.8fr,1fr] xl:items-start">
            
            {# COLONNE 1 : LOGO & SLOGAN #}
            <div class="col-span-1 lg:col-span-1">
                {# Logo agrandi (h-20) #}
                <img src="{{ asset('assets/img/logo-nonebackground.png') }}" 
                     alt="Lectis" 
                     class="h-20 w-auto mb-6 brightness-0 invert object-contain">
                
            
                <div class="flex gap-5 mt-8">
                    {% for social in ['facebook', 'instagram', 'tiktok', 'x-twitter', 'github'] %}
                        <a href="#" class="text-white/30 hover:text-white transition-all hover:-translate-y-1">
                            <i class="fa-brands fa-{{ social }} text-xl"></i>
                        </a>
                    {% endfor %}
                </div>
            </div>

            {# COLONNE 2 : LA LIBRAIRIE #}
            <div>
                <h3 class="text-[10px] font-black uppercase tracking-[0.3em] text-white/40 mb-8">La librairie</h3>
                <ul class="space-y-4">
                    <li><a href="{{path('app_a_propos')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">À propos</a></li>
                    <li><a href="{{path('app_contact')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">Contact</a></li>
                    <li><a href="{{path('app_f_a_q')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">Questions fréquentes</a></li>
                </ul>
            </div>

            {# COLONNE 3 : SERVICES #}
            <div>
                <h3 class="text-[10px] font-black uppercase tracking-[0.3em] text-white/40 mb-8">Services</h3>
                <ul class="space-y-4">
                    <li><a href="{{path('app_compte_lectis_plus')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">Lectis+</a></li>
                    <li><a href="{{path('app_retours')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">Retours</a></li>
                    <li><a href="{{path('app_remboursement')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">Remboursements</a></li>
                    <li><a href="{{path('app_mentions_legales')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">Mentions légales</a></li>
                </ul>
            </div>

            {# COLONNE 4 : JURIDIQUE #}
            <div>
                <h3 class="text-[10px] font-black uppercase tracking-[0.3em] text-white/40 mb-8">Conditions</h3>
                <ul class="space-y-4">
                    <li><a href="{{path('app_cgu')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">CGU</a></li>
                    <li><a href="{{path('app_cgv')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">CGV</a></li>
                    <li><a href="{{path('app_confidentialite')}}" class="text-sm font-bold text-white/80 hover:text-white transition-colors">Confidentialité</a></li>
                </ul>
            </div>

            {# COLONNE 5 : NEWSLETTER #}
            <div class="xl:justify-self-end xl:w-full xl:max-w-[260px]">
                <p class="mb-3 text-[10px] font-black uppercase tracking-[0.3em] text-white/40">Newsletter</p>
                <p class="mb-4 text-xs leading-relaxed text-[--bluegray-light] opacity-75">
                    Sélections et offres par email.
                </p>

                <form class="flex flex-col gap-2" data-newsletter-form>
                    <input type="email" placeholder="Votre email" class="min-w-0 rounded-lg border border-white/10 bg-white px-4 py-2.5 text-sm text-[--bluegray-dark] outline-none focus:ring-2 focus:ring-[--bluegray-medium]" data-newsletter-input required>
                    <button class="rounded-lg bg-[--bluegray-accent] px-4 py-2.5 text-sm font-bold text-white transition-all hover:bg-[--bluegray-hover]" type="submit">
                        S'inscrire
                    </button>
                </form>

                <p class="mt-2 hidden text-xs font-medium text-emerald-200" data-newsletter-success role="status" aria-live="polite">
                    Inscription prise en compte.
                </p>
            </div>
        </div>

        {# BAS DU FOOTER #}
        <div class="pt-8 flex flex-col md:flex-row justify-between items-center gap-6">
            <p class="text-[10px] font-black uppercase tracking-[0.2em] text-white/30">
                &copy; {{ "now"|date("Y") }} Lectis. Tous droits réservés.
            </p>

            <div class="flex items-center gap-8 text-white/20">
                <i class="fa-brands fa-cc-visa text-2xl"></i>
                <i class="fa-brands fa-cc-mastercard text-2xl"></i>
                <i class="fa-brands fa-cc-paypal text-2xl"></i>
                <i class="fa-brands fa-cc-apple-pay text-2xl"></i>
            </div>
        </div>
    </div>
</footer>
