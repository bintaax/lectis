<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Librio</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://kit.fontawesome.com/783766bfd8.js" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="../../public/assets/css/style.css">
</head>

<style>
@import "tailwindcss";
@import "flowbite/src/themes/default";
@plugin "flowbite/plugin";
@source "../../node_modules/flowbite";

@import url('https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&family=Playfair:ital,opsz,wght@0,5..1200,300..900;1,5..1200,300..900&display=swap');

:root {
  --bluegray-light:   #EEF3F8; /* Fond général */
  --bluegray-medium:  #CDD6E2; /* Sections secondaires */
  --bluegray-deep:    #7B8A9A; /* Icônes, contours, bordures */
  --bluegray-dark:    #2E3A45; /* Texte */
  --bluegray-accent:  #3F5366; /* Boutons, hover */
  
  --neutral-white:    #FFFFFF;
  --neutral-light:    #F7F9FC;
  --neutral-soft:     #E1E5EB;
}

body {
  font-family: 'Nunito', sans-serif;
  background-color: var(--bluegray-light);
  color: var(--bluegray-dark);
}

i {
  color: var(--bluegray-deep);
}

h1, h2, h3, h4, h5, h6 {
  font-family: 'Playfair', sans-serif;
}

button {
  background-color: var(--bluegray-accent);
  border: none;
  padding: 0.5rem 1rem;
  border-radius: 0.375rem;
  cursor: pointer;
}
</style>

    <header class="relative">
      <div class="mx-auto flex h-16 max-w-screen-xl items-center gap-8 px-4 sm:px-6 lg:px-8">
        <a class="block" href="#">
            <img src="{{ asset('assets/img/logo-nonebackground.png') }}" alt="Logo" style="width: 150px;">

        </a>

        <div class="flex flex-1 items-center justify-end md:justify-between">
          <!-- Menu desktop -->
          <nav aria-label="Global" class="hidden md:block">
            <ul class="flex items-center gap-6 text-sm">
              <li><a class="" href="{{path('app_home')}}">Accueil</a></li>
              <li><a class="" href="{{path('app_livres')}}">Catalogue</a></li>
              <li><a class="" href="#">Contact</a></li>
            </ul>
          </nav>
  
          <div class="flex items-center gap-4">
            <div class="sm:flex sm:gap-4">
                    {# Si l'utilisateur est bien connectée #}
        {% if app.user %}
            <div class="mb-3 text-sm text-gray-700" id=="login">
             {# Lien vers le panier d'un utilisateur #}
                <a type="button" href="{{path('app_panier')}}"><i class="fa-solid fa-basket-shopping" style="background-color:transparent;color:#7B8A9A;font-size:30px;"></i></a>              
            </div>
        {% endif %}

              <a class="block rounded-md px-5 py-2.5 text-sm font-medium text-white" href="{{ path('app_login') }}
" style="background-color:#3F5366;">
                Se connecter
              </a>
            </div>

            <!-- Bouton toggle -->
            <button id="menu-toggle" class="block rounded-sm bg-gray-100 p-2.5 text-gray-600 hover:text-gray-600/75 md:hidden">
              <span class="sr-only">Toggle menu</span>
              <svg xmlns="http://www.w3.org/2000/svg" class="size-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Menu mobile -->
      <nav id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-200">
        <ul class="flex flex-col items-center gap-4 py-4">
          <li><a class="text-gray-600 hover:text-[#664B4E]" href="#">Accueil</a></li>
          <li><a class="text-gray-600 hover:text-[#664B4E]" href="#">Catalogue</a></li>
          <li><a class="text-gray-600 hover:text-[#664B4E]" href="#">À propos</a></li>
          <li><a class="text-gray-600 hover:text-[#664B4E]" href="#">Contact</a></li>
        </ul>
      </nav>
    </header>
    