{% extends 'base.html.twig' %}

{% block body %}
<div class="mt-24 max-w-xl mx-auto p-6 bg-white rounded-xl shadow">

    <h1 class="text-2xl font-bold mb-6">Modifier mon mot de passe</h1>

    {{ form_start(form) }}

    {{ form_row(form.ancienPassword) }}
    {{ form_row(form.nouveauPassword) }}

    <button class="mt-4
     text-white px-6 py-2 rounded w-full">
        Mettre à jour
    </button>

    {{ form_end(form) }}

</div>
{% endblock %}
